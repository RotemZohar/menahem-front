import { Typography } from "@mui/material";
import React from "react";

const PetCarers = (props: { carers: any[] }) => {
  const { carers } = props;
  return (
    <div>
      <Typography sx={{ fontSize: "26px" }}>Not implemented yet</Typography>
    </div>
  );
};

export default PetCarers;
