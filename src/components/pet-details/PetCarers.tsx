import React from "react";

const PetCarers = (props: { carers: any[] }) => {
    const { carers } = props;
    return (
        <div>
            <h1>b</h1>
        </div>
    );
};

export default PetCarers;
